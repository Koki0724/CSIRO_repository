# @package _global_

# 1. 使うコンポーネントを強制的に指定 (override)
defaults:
  - override /feature: dino_giant
  - override /model: lightgbm
  - override /training: null
  - override /inference: null

normalization: null
# 3. 保存フォルダ名 (ファイル名と同じにするのが通例)
exp_name: "exp004_dino+lightgbm"
